import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Header, Footer } from "@/components";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Artistry Photography | Elegant Wedding & Event Photography",
  description: "Professional photography services for weddings and events. Capturing life's most precious moments with artistic vision and elegance.",
  keywords: ["wedding photography", "event photography", "professional photographer", "portrait photography"],
  authors: [{ name: "Artistry Photography" }],
  openGraph: {
    title: "Artistry Photography",
    description: "Professional photography services for weddings and events",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        suppressHydrationWarning
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
